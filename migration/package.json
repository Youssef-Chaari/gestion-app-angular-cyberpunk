{
  "name": "mysql-to-firestore-migration",
  "version": "1.0.0",
  "description": "Migration script to export MySQL data and import into Firestore",
  "main": "migrate.js",
  "scripts": {
    "migrate": "node migrate.js"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "firebase": "^12.9.0",
    "firebase-admin": "^11.0.0",
    "mysql2": "^3.3.0"
  }
}
